# Copy to secrets.toml for local dev, or paste into Streamlit Cloud → App settings → Secrets.

OPENAI_API_KEY = "sk-..."

# Optional: path to GloVe vectors for S²MATCH (not included in repo; file is ~330 MB).
# GLOVE_VECTORS_PATH = "/path/to/glove.6B.100d.txt"
